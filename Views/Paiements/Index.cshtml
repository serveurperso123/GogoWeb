@model IEnumerable<GogoDriverWeb.Models.Paiement>

@{
    ViewData["Title"] = "Index";
}
<div class="pagetitle">
    <h1>Les paiements</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"> <a asp-action="Index">Index</a></li>
            <li class="breadcrumb-item">Les paiements</li>
            <li class="breadcrumb-item active">Liste</li>
        </ol>
    </nav>
</div><!-- End Page Title -->
<p>
    <a asp-action="Create">Create New</a>
</p>
<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Liste paiements</h5>

                    <!-- Default Table -->
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.MomtantPaiement)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Telephone)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.DatePaiement)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.EtatPaiement)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Facturation)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.ModePaiementIdNavigation)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.MomtantPaiement)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Telephone)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DatePaiement)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EtatPaiement)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Facturation)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ModePaiementIdNavigation.ModePaiementId)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.PaiementId"><i class="fa fa-edit" aria-hidden="true"></i></a> |
                                        <a asp-action="Details" asp-route-id="@item.PaiementId"><i class="fa fa-list" aria-hidden="true"></i></a> |
                                        <a asp-action="Delete" asp-route-id="@item.PaiementId"><i class="fa fa-trash" aria-hidden="true"></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- End Default Table Example -->
                </div>
            </div>



        </div>
    </div>
</section>
